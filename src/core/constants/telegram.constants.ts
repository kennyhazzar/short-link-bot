export enum ACTIONS {
  setLanguage = 'language_',
  setSubscribe = 'subscribe_',
  showMedia = 'media_',
}

export enum COMMANDS {
  start = '/start',
  subscribe = '/sub',
  language = '/language',
}
